export default function AdminLoginLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>Admin Login - CarBook</title>
      </head>
      <body className="min-h-screen bg-base-200">
        {children}
      </body>
    </html>
  )
}